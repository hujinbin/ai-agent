import React from 'react';
import { Table } from 'antd';

const columns = [

]

function userTable(props) {
    return (
        <Table
            dataSource={[]}
            columns={columns}
        />
    )
}

export default userTable;